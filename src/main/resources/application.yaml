server:
  port: 8080
  servlet:
    context-path: "/api/v1"
spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5433/image-project'
    username: 'postgres'
    password: '132'
  jpa:
    properties:
      hibernate:
        dialect: 'org.hibernate.dialect.PostgreSQLDialect'
        format_sql: true
        hbm2ddl:
          auto: update
        show_sql: true
  liquibase:
    change-log: classpath:/db/changelog/db-changelog.xml
jwt:
  secret: oeidnfdsofsdofjdofijwdfoijwfojwfo
  expirationMs: 30
file:
  max-size: 10485760
  allowed-format: png, jpeg

minio:
  url: http://localhost:9000
  accessKey: user
  secretKey: password
  bucket: image-storage
  image-size: 10485760